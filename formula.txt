=IF(TRIM(A2)="", "Empty or Whitespace-only", 
IF(LEFT(A2,LEN(A2)-LEN(TRIM(A2)))<>"", "Leading Whitespace", 
IF(RIGHT(A2,LEN(A2)-LEN(TRIM(A2)))<>"", "Trailing Whitespace", 
IF(SEARCH("  ", A2), "Excessive Middle Whitespace", "No issue"))))
